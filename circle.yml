machine:
  php:
    version: 5.6.17
general:
  branches:
    ignore:
      - gh-pages 
test:
  override:
    - ./vendor/bin/phpunit --configuration phpunit.xml
    - ./vendor/bin/behat --config behat.yml
